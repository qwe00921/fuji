<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/white"
    android:orientation="vertical" >

    <RelativeLayout
        android:id="@+id/rl_mycenter_login_title"
        style="@style/bg_top_background"
        android:layout_marginBottom="18dip" >

        <TextView
            style="@style/bg_top_text"
            android:text="个人中心" >
        </TextView>

        <Button
            android:id="@+id/btn_mycenter_login_forgetpwd"
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:layout_alignParentRight="true"
            android:background="@drawable/state_press_btn_top_keyword" />
    </RelativeLayout>

    <TextView
        android:id="@+id/tv_mycenter_login_prompt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/rl_mycenter_login_title"
        android:layout_centerHorizontal="true"
        android:layout_margin="20dip"
        android:text="您还未绑定登陆，请登陆后继续操作"
        android:textColor="@color/grey"
        android:textSize="16sp" />

    <Button
        android:id="@+id/btn_mycenter_login"
        style="@style/btn_Login"
        android:layout_below="@+id/tv_mycenter_login_prompt"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="18dp"
        android:text="登    录" >
    </Button>

    <Button
        android:id="@+id/btn_mycenter_login_register"
        style="@style/btn_Login"
        android:layout_below="@+id/btn_mycenter_login"
        android:layout_marginTop="18dp"
        android:text="注册新会员" >
    </Button>

    <RelativeLayout
        android:id="@+id/rl_mycenter_mycenterlogin_bottommenu"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom" >
    </RelativeLayout>

</RelativeLayout>